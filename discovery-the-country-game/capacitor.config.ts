import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'discovery-the-country-game',
  webDir: 'www'
};

export default config;
